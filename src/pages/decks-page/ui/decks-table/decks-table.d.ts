import { Deck } from '@/services/decks/decks.types.ts';
type Props = {
    currentUserId?: string;
    decks?: Deck[];
};
export declare const DecksTable: ({ currentUserId, decks }: Props) => import("react/jsx-runtime").JSX.Element;
export {};
