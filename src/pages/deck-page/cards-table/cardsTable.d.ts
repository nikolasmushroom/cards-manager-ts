import { Card } from '@/services/decks/decks.types.ts';
type Props = {
    cards?: Card[];
    currentUserId?: string;
};
export declare const CardsTable: ({ cards, currentUserId }: Props) => import("react/jsx-runtime").JSX.Element;
export {};
